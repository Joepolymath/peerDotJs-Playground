var a = 'V';
console.log(a.charCodeAt());
// console.log('a'.charCodeAt());
// console.log('b'.charCodeAt());

// 😃
// 4 => 100 => 00000100
// UTF-8 => YOU MUST ALWAYS REPRESENT YOUR BINARY NUMBERS IN BYTES. 1 BYTE = 8BITS
// 100 => 00000100

// somestring@somestring.somestring

// HTTP => HTTPS
// TLS => TRANSPORT LAYER SECURITY
// HTTPS => HTTP + TLS
// HOW DOES TLS WORK?
/**
 * Assymetric keys => Public Key and Private Key
 * Public Keys are only used to encrypt data
 * Private Keys are used to decrypt data
 */
